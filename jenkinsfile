pipeline {
    agent any 
    parameters {
        string(name: 'PERSON', defaultValue: 'divya', description: 'Enter Your name')
    }
    stages {
        stage ('ParameterExample') {
            steps {
                echo "Welcome ${params.PERSON}"
            }
        }
    }
}
